{
  "name": "example-06-sdl-opengl",
  "version": "1.0.0",
  "private": "true",
  "description": "An example SDL and OpenGL web application written in C++.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "emscripten --clean ./src",
    "build": "emscripten --build --install ./src"
  },
  "devDependencies": {
    "emscripten-build": "^1.0.1"
  }
}
