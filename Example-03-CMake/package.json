{
  "name": "example-03-cmake",
  "version": "1.0.0",
  "private": "true",
  "description": "A C++ \"Hello world\" web application built with CMake.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "emscripten --clean",
    "build": "emscripten --build --install"
  },
  "devDependencies": {
    "emscripten-build": "^1.0.1"
  }
}
