{
  "name": "example-04-autotools",
  "version": "1.0.0",
  "private": "true",
  "description": "A C++ \"Hello world\" web application built with Autotools.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "node ./clean.js",
    "build": "cd ./src && autoreconf --install --force && cd .. && node ./build.js"
  },
  "devDependencies": {
    "emscripten-build": "git+https://github.com/devappd/emscripten-build-npm#main"
  }
}
