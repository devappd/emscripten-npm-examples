{
  "name": "example-01-helloworld",
  "version": "1.0.0",
  "private": "true",
  "description": "A C++ \"Hello world\" web application.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd ./src && emscripten em++ -o ../dist/index.html ./main.cpp"
  },
  "devDependencies": {
    "emscripten-build": "git+https://github.com/devappd/emscripten-build-npm#main"
  }
}
